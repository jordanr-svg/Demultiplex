args: 
  fwd_read: R1
  fwd_bar: R2
  rev_bar: R3
  rev_read: R4
  
rev_compliment:(seq: str) -> str:
  ''' Takes string of sequence, reverse sequence and turns A to T, T to A, G to C, C to G, and N to N '''
  return rev_str

barcodes_set = create set with barcodes as keys
loop over barcode file and put barcodes in.

pairs = empty dictionary
populate pairs with every possible barcode variation, key is barcode pair value is count

open all four files:
  for every sequence R1, R2, R3, R4:
    rev_ string = R3.rev_compliment(seq)
    seq_string = R2.seq
    line1 = header
    pair = ({seq_string}-{seq})
    header.append(pair)
  if N in rev_string or seq_string:
    write entire read from R1 to R1_unk
    write entire read from R4 to R2_unk
    increment unk counter
  if pair in pairs dictionary:
      count pair in dictionary
      write entire read from R1 to R1_{barcode}
      write entire read from R4 to R2_{barcode}
    if rev_string OR seq_string in barcode_set:
        write entire read from R1 to R1_hopped
        write entire read from R4 to R2_hopped
        increment hopped counter
  else:
      write entire read from R1 to R1_unk
      write entire read from R4 to R2_unk
      increment unk counter



Define the problem:
- We have Illumina sequencing data for multiple projects/samples/labs on one run. In order to identify samples barcodes were added to both ends of the sequences. Barcodes at both ends helps determine if index hopping occured where during sequence amplification the barcodes from a different sample ended up in the sequence. This code will go through the sequence of each read and the corresponding barcode reads to identify who the sample belongs too and if the indexes were hopped.

Determine/describe what output would be informative:
- Since we have 24 barcodes we expect 48 read files, 24 forward read and 24 reverse and count how many reads for each barcode. We will also make two index hopped files for forwaard and reverse reads, and two unknown files for forward and reverse reads.

Write examples (unit tests!):
Include four properly formatted input FASTQ files with read pairs that cover all three categories (dual matched, index-hopped, unknown index)
Include the appropriate number of properly formatted output FASTQ files given your input files
      
